version: '3.8'

services:
  ytb-mp3-download-dev:
    image: ytb-mp3-download:dev
    container_name: ytb-mp3-download-dev
    build:
      dockerfile: Dockerfile
    ports:
      - '5005:5005'
    volumes:
      - .:/app
